{
	"$schema": "http://json-schema.org/schema",
	"cli": "nx",
	"$id": "api",
	"type": "object",
	"desc": "Generates an api/sdk that is exposed on the projects express api. ",
	"properties": {
		"name": {
			"type": "string",
			"description": "API name",
			"visible": true,
			"x-prompt": "What name would you like to use for the API?",
			"$default": {
				"$source": "argv",
				"index": 0
			}
		}
	},
	"required": ["name"]
}
